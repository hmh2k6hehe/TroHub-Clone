{
  "name": "trohub-web-only",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TroHub web admin with a local API and optional MongoDB storage.",
  "scripts": {
    "dev": "python3 -m http.server 5173 --bind 127.0.0.1",
    "dev:api": "cd API_DuAnTotNghiep && npm start",
    "start": "python3 -m http.server 5173 --bind 127.0.0.1"
  },
  "keywords": [
    "trohub",
    "web-admin",
    "room-rental",
    "vanilla-js"
  ],
  "license": "MIT",
  "optionalDependencies": {
    "mongodb": "^6.20.0"
  },
  "dependencies": {
    "acorn": "^8.17.0"
  }
}
